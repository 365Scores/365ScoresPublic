on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        options:
          - Production
          - Staging

jobs:
  job:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment }}
    steps:
      - run: echo "VARIABLE is $VARIABLE"
        # likely will still be empty
